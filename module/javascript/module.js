// Expose the native API to javascript
forge.background_geolocation = {
    //showAlert: function (text, success, error) {
    //    forge.internal.call('background_geolocation.showAlert', {text: text}, success, error);
    //}
};

// Register for our native event
//Forge.internal.addEventListener("background_geolocation.resume", function () {
//	alert("Welcome back!");
//});
